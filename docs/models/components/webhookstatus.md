# WebhookStatus

## Example Usage

```python
from mag3nt.models.components import WebhookStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: WebhookStatus = "ACTIVE"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"ACTIVE"`
- `"DISABLED"`
