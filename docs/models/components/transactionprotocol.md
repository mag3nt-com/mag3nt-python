# TransactionProtocol

## Example Usage

```python
from mag3nt.models.components import TransactionProtocol

# Open enum: unrecognized values are captured as UnrecognizedStr
value: TransactionProtocol = "x402"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"x402"`
- `"ap2"`
- `"mpp"`
