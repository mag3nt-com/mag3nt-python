# FundingVerifyStatus

## Example Usage

```python
from mag3nt.models.operations import FundingVerifyStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: FundingVerifyStatus = "confirmed"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"confirmed"`
- `"pending"`
